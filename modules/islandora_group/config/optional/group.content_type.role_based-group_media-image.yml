langcode: en
status: true
dependencies:
  config:
    - group.type.role_based
    - media.type.image
  module:
    - groupmedia
    - media
id: role_based-group_media-image
label: 'Role Based: Group media (Image)'
description: 'Adds <em class="placeholder">Image</em> content to groups both publicly and privately.'
group_type: role_based
content_plugin: 'group_media:image'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
