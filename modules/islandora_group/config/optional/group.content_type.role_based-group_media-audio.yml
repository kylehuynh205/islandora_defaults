langcode: en
status: true
dependencies:
  config:
    - group.type.role_based
    - media.type.audio
  module:
    - groupmedia
    - media
id: role_based-group_media-audio
label: 'Role Based: Group media (Audio)'
description: 'Adds <em class="placeholder">Audio</em> content to groups both publicly and privately.'
group_type: role_based
content_plugin: 'group_media:audio'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
